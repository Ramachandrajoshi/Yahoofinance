{
  "name": "com.nsplus.yahoofinance",
  "displayName": "HellowWorld",
  "version": "1.0.0",
  "description": "A sample Apache Cordova application that responds to the deviceready event.",
  "main": "index.js",
  "scripts": {
    "prestart": "cd ../Vue_frontEnd && npm run build && cp -r dist/* ../Cordova_app/www/",
    "start": "cordova prepare && cordova emulate android"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova-android": "^10.1.2"
  },
  "cordova": {
    "platforms": [
      "android"
    ]
  }
}